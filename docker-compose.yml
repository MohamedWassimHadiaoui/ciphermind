version: "3.8"

services:
  ciphermind:
    build: .
    container_name: ciphermind-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_db:/app/chroma_db
    restart: unless-stopped
